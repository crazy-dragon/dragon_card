# 🐉 DragonCard UI 界面设计文档

| 版本号 | 日期 | 修改说明 |
| :--- | :--- | :--- |
| v2.0 | 2026-08 | 基于当前实现重写，反映实际 UI 结构 |

本文档描述 **DragonCard** 当前界面的视觉规范、布局结构与交互设计。作为后续开发与迭代的设计依据。

---

## 1. 整体布局

采用「左侧全局导航 + 右侧主内容区」的 SPA 布局。全局导航在主页与学习视图间保持，学习视图使用独立的专注布局。

```
┌──────────────────────────────────────────────────────────────┐
│ Global Sidebar │  Main Area                                   │
│ ┌────────────┐ │  ┌────────────────────────────────────────┐  │
│ │ 🐉 品牌    │ │  │ 主页(Home Panel) / 学习视图(Study View) │  │
│ │ 我的卡组    │ │  │                                        │  │
│ │ 成就        │ │  │  主页：顶栏 + 统计卡 + 卡组网格         │  │
│ │ 统计        │ │  │  学习：侧栏tabs + 顶栏 + 目录/卡片区    │  │
│ │ 市场        │ │  │                                        │  │
│ │ 使用文档    │ │  │                                        │  │
│ │ 先贤的智慧  │ │  │                                        │  │
│ └────────────┘ │  └────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

### 全局侧边栏 (Global Sidebar)

- **品牌区**：Dragon 图标 + "DragonCard" 标题
- **导航项**：我的卡组 / 成就 / 统计 / 市场 / 使用文档（Font Awesome 图标 + 文字，当前项高亮主题色）
- **附加项**：先贤的智慧（彩蛋按钮，点击弹出古文智慧弹窗）

---

## 2. 主页 — 我的卡组 (Home Panel)

### 2.1 主页顶栏 (Home Header)

| 元素 | 说明 |
|------|------|
| 左侧标题 | 页面标题（我的卡组 / 成就中心 / 数据统计 …）|
| 关于按钮 | 圆形信息图标，弹出 About 弹窗 |
| 深色模式 | 月亮/太阳图标，切换深色主题 |
| 语言切换 | `中/EN` 按钮，切换中英界面 |
| API 参考 | `</>` 图标，弹出「模板 API 参考」 |
| 新建卡组 | 主色实心按钮（+ 新建卡组）|

### 2.2 统计概览卡 (Stat Cards)

3 张横向卡片，各含图标 + 数值 + 标签：

| 卡片 | 图标 | 数值颜色 |
|------|------|---------|
| 卡组总数 | 图层图标 | 主色蓝 |
| 卡片总数 | 卡片图标 | 成功绿 |
| 连续学习天数 | 火焰图标 | 危险红 |

数值由后端实时计算（连续学习天数来自 `/v1/achievements` 的 `streak_days`）。

### 2.3 卡组网格 (Deck Grid)

响应式网格：**>900px 3 列，641-900px 2 列，<640px 1 列**。

每张卡组卡片：

```
┌──────────────────────────────────────┐
│          [⚙][📊]  (hover 显示管理按钮)│
│ ┌────┐ 卡组名称                       │
│ │类型│ 类型标签(图标+文字)             │
│ │图标│                                 │
│ └────┘                                 │
│ 模板描述                              │
│ 已掌握 X / Y        ⏱ 上次学习        │
│ ▓▓▓▓▓▓░░ 进度条                      │
│ ───────────────────────────           │
│ [模板名]                              │
└──────────────────────────────────────┘
```

- **类型图标**：语言/知识/逻辑/技能/其它 各配 Font Awesome 图标 + 专属配色背景
- **进度条**：掌握度 >70% 绿、>40% 橙、否则蓝
- **管理按钮**：悬停显示（掌握统计📊 / 管理⚙）
- **底部模板徽章**：显示当前模板名，未绑定显示灰色"未绑定模板"

### 2.4 新建卡组弹窗

- 名称输入框
- 类型选择：5 个横向图标按钮（图标+文字），选中高亮对应主题色
- 提示文字：创建后可点击「管理卡组」上传模板与数据

---

## 3. 学习视图 (Study View)

进入卡组后全屏专注，布局与主页不同：

```
┌────────────────────────────────────────────┐
│ 侧栏(160px)  │  主内容区                    │
│ ┌──────────┐ │  ┌─────────────────────────┐ │
│ │🐉Dragon  │ │  │ 顶栏: 返回 标题 [A][🎤] │ │
│ │           │ │  │      [↑][↓][单卡][刷新] │ │
│ │ 目录Tab   │ │  ├─────────────────────────┤ │
│ │ P_001     │ │  │ 目录页/分页卡片         │ │
│ │ P_002     │ │  │  或 学习卡片列表        │ │
│ └──────────┘ │  │                         │ │
└──────────────┘  └─────────────────────────┘ │
```

### 3.1 侧栏 (Sidebar)

- **DragonCard 品牌按钮**：点击回到目录页（catalogue）
- **学习页 Tabs**：已打开的学习页，当前页高亮
  - 未掌握页（study-tab）：黄色系
  - 已掌握页（review-tab）：绿色系
  - × 关闭按钮触发"完成学习"确认

### 3.2 顶栏 (Top Bar)

| 元素 | 说明 |
|------|------|
| 返回 | 返回主页（触发确认弹窗）|
| 卡组标题 | 当前卡组名 |
| 字体设置 | A 按钮，弹出字号调节抽屉 |
| 语音选择 | 麦克风，语音按语言分组 |
| 滚动 | 上/下箭头，滚到卡片首尾 |
| 单卡模式 | 网格图标，切换单卡专注模式 |
| 刷新统计 | 未知/总数 徽章，点击刷新 |

### 3.3 目录页 (Catalogue)

- 分页网格（每页 100 张，10 列）
- 未掌握页黄色标记，已掌握页绿色标记

### 3.4 卡片区域

- 卡片由模板渲染（HTML/CSS/JS 三件套），卡片内容由模板决定
- 每张卡片固定有操作按钮区（模板定义）：发音 / 标记 / 收藏 / 切换释义
- 单卡模式：一次一张，左右箭头/空格翻卡，带入场动画

---

## 4. 管理卡组弹窗 (Management Modal)

弹窗分为头部 + 主体两部分。

### 4.1 头部

- 卡组类型图标（大图标 + 类型色）
- 卡组名（可点击改名）
- 副标题：当前模板名 + 模板描述
- 类型下拉：切换卡组类型
- 关闭按钮

### 4.2 主体

**顶部并排两区：**

| 模板区 | 数据区 |
|--------|--------|
| 标题「模板 · 最多 3 个」 | 标题「数据」 |
| 最多 3 个模板槽位 | 条数统计 |
| 每个槽：模板名 + 替换/导出按钮 + 当前模板圆点 | 上传 / 导出 / 重新打乱按钮 |
| 空槽显示「+ 上传模板」 | |
| 点击卡片 → 右侧预览 | |

**底部预览区：**

- 点击模板槽位 → 实时渲染该模板的卡片预览
- 无样本数据时显示占位提示

---

## 5. 统计与成就页

### 5.1 数据统计页

- **控制栏**：卡组筛选下拉 + 视图切换（热力图/日/周/月）+ 日期导航 + 刷新
- **统计卡**：各 action 类型计数
- **图表区**：全年热力图（按活动量着色，可点击跳转日视图）或柱状堆叠图
- **图例**：action 类型与颜色映射

### 5.2 成就页

按分组展示成就卡片：

- **称号 · 按卡组**：👑 + 称号名（学徒/熟练者/专家…按轮次升级）
- **持之以恒 · 连续学习**：连续天数成就
- **开疆拓土 · 卡组**：卡组数量成就
- **妙手偶得 · 互动**：发音/标记次数成就

每张成就卡：图标 + 名称 + 描述 + 进度条 + 数值/目标。

### 5.3 掌握统计弹窗

- **热力图**：按卡组顺序着色（已掌握绿 / 未掌握红）
- **掌握分布**：按状态分类
- **轮次统计**：中轴对称金字塔展示每轮学习时间与标记数量

---

## 6. 视觉规范

### 6.1 主题色（CSS 变量）

主页（蓝色系，`--hp-*`）与学习视图（青绿系，`--study-*`）使用不同色板，通过 `body.study-mode` / `body.dark-mode` 切换。

| 用途 | 主页变量 | 说明 |
|------|---------|------|
| 主色 | `--hp-primary` | 链接、激活态、主按钮 |
| 成功 | `--hp-success` | 完成、已掌握 |
| 危险 | `--hp-danger` | 错误、未掌握 |
| 文字 | `--hp-text` / `--hp-text-sub` / `--hp-text-light` | 三级文字 |
| 边框 | `--hp-border` | 分隔线、卡片边框 |
| 卡片 | `--hp-card` | 卡片背景 |
| 阴影 | `--hp-shadow` / `--hp-shadow-hover` | 卡片阴影 |

### 6.2 字体

- 字体族：`Inter, -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif`
- 基础字号：14px；标题 16-20px 加粗

### 6.3 圆角与阴影

| 元素 | 值 |
|------|-----|
| 卡片 | 12px |
| 按钮/标签 | 6-10px |
| 图标按钮 | 圆形（90px）|
| 弹窗 | 12-16px |

### 6.4 图标

- 全局使用 **Font Awesome**（本地加载 v7）：`fa-solid fa-*`
- 操作按钮图标：发音 `fa-volume-high`、标记 `fa-star`、收藏 `fa-bookmark`、切换 `fa-eye`
- 类型图标：语言 `fa-language`、知识 `fa-book-open`、逻辑 `fa-brain`、技能 `fa-hammer`、其它 `fa-ellipsis`

### 6.5 动画

- 卡片入场：`cardIn`（上移 + 淡入，可逐卡延迟）
- 卡片悬停：`translateY(-2px)` 上浮 + 阴影加深
- 页面切换：淡入
- Tab 抖动：`shake`（重复点击已开页）

---

## 7. 国际化

- 文案全部走 `i18n.js` 字典（中英 200+ key）
- 静态元素用 `data-i18n` / `data-i18n-tooltip` / `data-i18n-placeholder` 标记
- 动态内容用 `t('key', vars)` 函数
- 语言切换：`toggleLang()` 刷新当前视图（主页/学习/统计/成就/文档）
- **古文内容不翻译**：确认短语「广修万劫证吾道心」、先贤智慧引文保持原文

---

## 8. 响应式断点

| 断点 | 行为 |
|------|------|
| >900px | 卡组 3 列 |
| 641-900px | 卡组 2 列 |
| <640px | 卡组 1 列，统计卡 1 列，全局侧边栏收窄 |

---

## 9. 设计注意事项

1. 卡片本体样式由模板定义，框架只提供容器与操作区规范，不干预模板 CSS。
2. 模板颜色优先用项目 CSS 变量（`var(--card-bg)`、`var(--ink)` 等）以适配暗色模式。
3. Tailwind 工具类由本地 Play CDN 动态编译，模板内直接书写工具类，勿用 `@apply`。
4. 所有弹窗/抽屉在切换主题时需保持变量驱动的配色。
5. 新增页面/弹窗时，文案务必接入 i18n 字典。
